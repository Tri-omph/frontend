export enum UserStatusEnum {
  RESTREINT = "restreint",
  SIMPLE = "simple",
  ADMIN = "admin",
  TOUS = "tous",
}

export enum UserFilter {
  PSEUDO = "pseudo",
  POINTS = "points",
  STATUS = "statut",
}
